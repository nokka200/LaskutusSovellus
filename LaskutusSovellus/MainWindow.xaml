<Window x:Class="LaskutusSovellus.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LaskutusSovellus"
        mc:Ignorable="d"
        Title="Laskutussovellus" Height="500" Width="300">

    <Window.DataContext>
        <local:InvoiceHolder/>
    </Window.DataContext>
    
    <Grid ShowGridLines="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Invoices}" AutoGenerateColumns="False" x:Name="DtgMain">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" IsReadOnly="True" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Tilaaja" Width="*" Binding="{Binding AddressBiller}"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Click="OpenInformationWindow" Content="Open" />
    </Grid>
</Window>
